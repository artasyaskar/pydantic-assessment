[task]
name = "artasyaskar/validate-default-factory-fix"
description = "Fix validate_default behavior with default_factory taking validated data"
repository = "pydantic"
type = "swe-bench"

[requirements]
python = ">=3.9"
dependencies = ["pydantic", "pydantic-core"]

[evaluation]
test_framework = "pytest"
test_path = "tests/"
